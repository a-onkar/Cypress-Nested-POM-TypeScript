{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "ng lint",
    "lint:fix": "ng lint --fix",
    "e2e": "start-server-and-test start http-get://localhost:4200 runcy -- --config-file cypress/cypress.json - NEEDS UPDATE",
    "e2e-ci": "npm run runcy -- --config-file cypress/cypress-ci.json - NEEDS UPDATE",
    "opency": "./node_modules/.bin/cypress open --project e2e --config-file cypress/cypress.json - NEEDS UPDATE",
    "runcy": "./node_modules/.bin/cypress run --project e2e - NEEDS UPDATE"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.11.1",
    "typescript": "^4.0.2"
  },
  "devDependencies": {
    "cypress": "^5.2.0",
    "cypress-plugin-retries": "^1.5.2",
    "cypress-plugin-tab": "^1.0.5",
    "cypress-xpath": "^1.6.0",
    "jasmine-core": "~3.4.0",
    "jasmine-spec-reporter": "~4.2.1",
    "tslint": "~6.1.0",
    "typescript": "^4.0.2"
  }
}
